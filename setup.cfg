# setup.cfg is the only file that can configure all used linters and checkers
# (flake8 is pretty restrictive when it comes to what configuration files it
# supports: .flake8, setup.cfg and tox.ini, that's it).

[mypy]

[pylint.design]
max-attributes=10
max-public-methods=25

[flake8]
